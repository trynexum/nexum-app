{
  "name": "nexum-base-sepolia",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --studio nexum",
    "create-local": "graph create --node http://localhost:8020/ nexum",
    "remove-local": "graph remove --node http://localhost:8020/ nexum",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 nexum"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.98.1",
    "@graphprotocol/graph-ts": "^0.35.1"
  }
}
